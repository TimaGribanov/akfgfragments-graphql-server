ktor:
  deployment:
    port: 9000
  application:
    modules:
      - com.akfgfragments.ApplicationKt.module
mariadb:
  driverClassName: "org.mariadb.jdbc.Driver"
  url: "jdbc:mariadb://<host>:<port>/<schema>"
  user: "<user>"
  password: "<password>"